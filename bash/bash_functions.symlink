 #stupid functions
 randnum() {
    read x;
    echo $[ $RANDOM % $x];
 }

 gun() { 
    [ $[ $RANDOM % 6 ] == 0 ] && echo BANG || echo CLICK;
 }

gpgfile() {
    tar -cz $1 | gpg -c -o $1.tgz.gpg
}
degpgfile() {
    gpg -d $1 | tar xz
}


